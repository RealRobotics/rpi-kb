# Get the camera working on a Pi5 running Ubuntu 24.04LTS

I had hoped that Ubuntu 24.04LTS would include support for the cameras but unfortunately this is not the case.  This is how I fixed it.

##


## Script

Having fixed it, I then created a script
